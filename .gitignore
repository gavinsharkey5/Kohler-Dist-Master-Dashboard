__pycache__/
*.pyc

# merge_export.py / update_data.py safety copies — local only
*.csv.bak

# wine-spirits/backups/<timestamp>/ — pre-merge copies of the masters. Local
# only: git history is the durable backup (the previous CSVs are in the last
# commit), so there is no reason to carry ~7 MB of duplicates in the repo.
wine-spirits/backups/

# Sign-in allow list built from the Encompass users export -- personal
# emails and phone numbers. The repo is public: never commit this.
supabase/data/
